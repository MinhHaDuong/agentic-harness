%erg v1
Title: Add set -euo pipefail to all hook scripts
Created: 2026-04-23
Author: claude
Closed: migrated from Status: closed

--- log ---
2026-04-23T12:30Z claude created — celebrate sweep on 0015; 7 scripts lack error handling
2026-04-24T00:00Z claude note reimagine — 6 scripts, not 7: shell-init.sh is sourced (not executed), adding pipefail would break interactive shells. lint-on-edit.sh and warn-stale-rules.sh need || true on ruff/grep calls. CI check: grep -L with named exclusion for shell-init.sh.
2026-04-24T00:00Z claude note plan — 3 scripts need || true (not 2): guard-commit-on-main.sh:19 (git branch in non-git context), on-start.sh:11 (grep returns 1 on all-empty .env), lint-on-edit.sh:17,20 (ruff returns 1 on violations). CI job: pipefail-guard with self-test fixture mirroring leak-guard pattern.
2026-04-24T09:52Z claude status closed — PR #56 merged (squash), all exit criteria met

--- body ---
## Context

/celebrate sweep after PR #51 (CI workflow) found 7 hook scripts missing
`set -euo pipefail`. Silent failures in hook scripts are dangerous: a broken
guard (e.g., `guard-commit-on-main.sh`) may exit 0 despite an error,
silently allowing what it was meant to block.

Affected files:
- scripts/guard-destructive-bash.sh
- scripts/block-pr-merge-in-worktree.sh
- scripts/shell-init.sh
- scripts/guard-commit-on-main.sh
- scripts/lint-on-edit.sh
- scripts/warn-stale-rules.sh
- scripts/on-start.sh

## Actions

1. Add `set -euo pipefail` as the second line (after the shebang) in each
   script. Review each for commands that legitimately may fail (grep, test)
   and add `|| true` where needed.
2. Add a CI check to `CI.yml` (requires 0015, now merged): grep all
   `scripts/*.sh` for `set -euo pipefail`; fail if any are missing.
   This becomes the standing regression guard.

## Test

Remove `set -euo pipefail` from one script; expect CI red.

## Exit criteria

- All `scripts/*.sh` have `set -euo pipefail`.
- CI job enforces it.
- No hook regression (manual smoke: trigger each hook scenario once).
